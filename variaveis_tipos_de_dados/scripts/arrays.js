/*Aqui nos criamos um array que temos alguns numeros
esses numeros estao armazenados*/
var numeros = [1, 2, 3, 4, 5];

/*se colocarmos desse jeito sem realizar o devido tratamento 
ira aparecer uma lista comtodos os numeros*/
console.log(numeros);

/*Agora iremos printa o array com o devido tratamento fazendo 
aparecer apenas a informação que queremos, para fazer isso devemos
informar qual campo se encontra ele */
console.log(numeros[2]);

/*Agora iremos mostra um forma para altera um numero no array
lembrando que o array começa no numero zero. para isso precisamos 
pegar o nome do array e passar a possição que queremos altera*/
numeros[2] = 1525854798548;
console.log(numeros[2]);

/*Com um arrey tambem podemos colocar muitl tipo de dados como 
o exemplo abaixo*/
var multdados = ['string', 11, false];
console.log(multdados);

